#include<stdio.h>
int main(){
    float a, b, product;
    printf("enter two float number:");
    scanf("%f %f",&a,&b);
    product= a * b ;
    printf("product is %f",product);
    return 0;
}